kconfig patch: confdata.c
-------------------------

This patch removes the timestamp from ptxconfig files,
which makes svn management of ptxdist projects a bit
cleaner

bbu, 20071019

------------------------------------------------------------------------------

Index: confdata.c
===================================================================
--- confdata.c	(revision 7549)
+++ confdata.c	(working copy)
@@ -311,11 +311,8 @@
 	fprintf(out, _("#\n"
 		       "# Automatically generated make config: don't edit\n"
 		       "# %s version: %s\n"
-		       "%s%s"
 		       "#\n"),
-		     getenv("PROJECT"), getenv("FULLVERSION"),
-		     use_timestamp ? "# " : "",
-		     use_timestamp ? ctime(&now) : "");
+		     getenv("PROJECT"), getenv("FULLVERSION"));
 	if (out_h)
 		fprintf(out_h, "/*\n"
 			       " * Automatically generated C config: don't edit\n"
