#
# Submitted-By: Sascha Hauer, 2006-08-21
# Committed-By: Sascha Hauer
#
# Description:
#
# When testing for pthread, being able to compile a test program should be
# enough. Do not try to execute it as it brakes crosscompiling
#
# State:
#
# ?
#
Index: MPlayer-1.0pre7try2/configure
===================================================================
--- MPlayer-1.0pre7try2.orig/configure
+++ MPlayer-1.0pre7try2/configure
@@ -2710,13 +2710,12 @@ elif hpux ; then
   _ld_pthread=''
 elif ( cc_check && $TMPO ) ; then              # QNX
   _ld_pthread=' ' # _ld_pthread='' would disable pthreads, but the test worked
-elif ( cc_check -lpthread && $TMPO ) ; then
+elif ( cc_check -lpthread ) ; then
   _ld_pthread='-lpthread'
-elif ( cc_check -pthread && $TMPO ) ; then
+elif ( cc_check -pthread ) ; then
   _ld_pthread='-pthread'
 else
   if test "$_ld_static" ; then
-    # for crosscompilation, we cannot execute the program, be happy if we can link statically
     if ( cc_check -lpthread ) ; then
       _ld_pthread='-lpthread'
     elif ( cc_check -pthread ) ; then
