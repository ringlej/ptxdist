From: Marc Kleine-Budde <mkl@pengutronix.de>
Subject: remove test for _local_ path from configure

This patch removes a broken check for _local_ path from configure.

Signed-off-by: Marc Kleine-Budde <mkl@penutronix.de>

---
 configure |   14 +++++++-------
 1 file changed, 7 insertions(+), 7 deletions(-)

Index: ltrace-0.5/configure
===================================================================
--- ltrace-0.5.orig/configure
+++ ltrace-0.5/configure
@@ -1,4 +1,4 @@
-#! /bin/sh
+r! /bin/sh
 # Guess values for system-dependent variables and create Makefiles.
 # Generated by GNU Autoconf 2.59 for ltrace 0.5.
 #
@@ -3508,12 +3508,12 @@ fi
 done
 
 
-for path in /usr/include/elfutils /usr/local/include/elfutils \
-	    /usr/include/libelf /usr/local/include/libelf; do
-	if test -f ${path}/gelf.h; then
-		CPPFLAGS="$CPPFLAGS -I ${path}"
-	fi
-done
+#for path in /usr/include/elfutils /usr/local/include/elfutils \
+#	    /usr/include/libelf /usr/local/include/libelf; do
+#	if test -f ${path}/gelf.h; then
+#		CPPFLAGS="$CPPFLAGS -I ${path}"
+#	fi
+#done
 
 if test "${ac_cv_header_gelf_h+set}" = set; then
   echo "$as_me:$LINENO: checking for gelf.h" >&5
