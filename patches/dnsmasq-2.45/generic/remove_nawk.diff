Not all systems provide nawk. awk seems also do the job.

---
 build-target/dnsmasq-2.45/Makefile |    2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

Index: build-target/dnsmasq-2.45/Makefile
===================================================================
--- build-target/dnsmasq-2.45/Makefile.orig
+++ build-target/dnsmasq-2.45/Makefile
@@ -23,7 +23,7 @@ PO = po
 MAN = man
 
 PKG_CONFIG = pkg-config
-AWK = nawk
+AWK = awk
 INSTALL = install
 
 DBUS_MINOR=" `echo $(COPTS) | ../bld/pkg-wrapper $(PKG_CONFIG) --modversion dbus-1 | $(AWK) -F . -- '{ if ($$(NF-1)) print \"-DDBUS_MINOR=\"$$(NF-1) }'`" 
