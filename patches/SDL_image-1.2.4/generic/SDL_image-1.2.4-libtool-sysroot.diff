#
# Submitted-By: Robert Schwebel, 2006-03-04
# Committed-By: Robert Schwebel
#
# Error:
#
# -
#
# Description:
#
# libtool picks up libraries from the host system. 
#
# State:
# 
# libtool should be fixed. Patch not to be sent upstream. 
#

diff -urN SDL_image-1.2.4-orig/configure SDL_image-1.2.4/configure
--- SDL_image-1.2.4-orig/configure	2004-12-16 05:41:33.000000000 +0100
+++ SDL_image-1.2.4/configure	2006-03-04 22:41:23.000000000 +0100
@@ -20950,7 +20950,7 @@
   echo $ECHO_N "(cached) $ECHO_C" >&6
 else
   ac_check_lib_save_LIBS=$LIBS
-LIBS="-lpng -lz $LIBS"
+LIBS="-Wl,-lpng -Wl,-lz $LIBS"
 cat >conftest.$ac_ext <<_ACEOF
 /* confdefs.h.  */
 _ACEOF
@@ -21026,7 +21026,7 @@
   echo $ECHO_N "(cached) $ECHO_C" >&6
 else
   ac_check_lib_save_LIBS=$LIBS
-LIBS="-lz  $LIBS"
+LIBS="-Wl,-lz  $LIBS"
 cat >conftest.$ac_ext <<_ACEOF
 /* confdefs.h.  */
 _ACEOF
@@ -21098,7 +21098,7 @@
     fi
     if test x$have_libpng = xyes -a x$have_libz = xyes; then
         CFLAGS="$CFLAGS -DLOAD_PNG"
-        IMG_LIBS="$IMG_LIBS -lpng -lz"
+        IMG_LIBS="$IMG_LIBS -Wl,-lpng -Wl,-lz"
     else
         { echo "$as_me:$LINENO: WARNING: PNG image loading disabled" >&5
 echo "$as_me: WARNING: PNG image loading disabled" >&2;}
