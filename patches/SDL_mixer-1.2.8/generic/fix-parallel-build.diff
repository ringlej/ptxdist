From: Marc Kleine-Budde <mkl@pengutronix.de>
Subject: fix parallel build

We have to create the objects dir before saving gcc's output into it.

Signed-off-by: Marc Kleie-Budde <mkl@pengutronix.de>
---
 Makefile.in |    2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

Index: SDL_mixer-1.2.8/Makefile.in
===================================================================
--- SDL_mixer-1.2.8.orig/Makefile.in
+++ SDL_mixer-1.2.8/Makefile.in
@@ -76,7 +76,7 @@ depend:
 
 include $(depend)
 
-$(objects)/$(TARGET): $(OBJECTS)
+$(objects)/$(TARGET): $(OBJECTS) $(objects)
 	$(LIBTOOL) --mode=link $(CC) -o $@ $(OBJECTS) $(LDFLAGS) $(EXTRA_LDFLAGS) $(LT_LDFLAGS)
 
 install: all install-hdrs install-lib #install-bin
