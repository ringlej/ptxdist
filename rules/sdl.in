menuconfig SDL
	bool
	prompt "SDL                   "
	help
	  FIXME

config SDL_LIB
	bool
	prompt "SDL Base library"
	depends on SDL
	help 
	  FIXME

config SDL_VIDEO_X
	bool
	prompt "build SDL with X support"
	depends on BROKEN
	default no
	depends on SDL
	select XORG_LIB_X11
	help
	  FIXME

comment "sdl with X is broken, please send a patch"
	depends on SDL
	depends on !BROKEN

source "rules/sdl_image.in"
