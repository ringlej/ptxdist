menuconfig SDL
	bool
	depends on BROKEN
	prompt "SDL                   "
	help
	  FIXME

config SDL_LIB
	bool
	prompt "SDL Base library"
	depends on SDL
	help 
	  FIXME

source "rules/sdl_image.in"

comment "sdl is broken, please send a patch"
	depends on !BROKEN
