menu	"VHLL Support        "

comment "Python requires libdl on the target (see C Library)"
	depends on !GLIBC_DL && !UCLIBC_DL

config VHLL_PYTHON23
	bool
	prompt "Python 2.3"
	depends on GLIBC_DL || UCLIBC_DL

endmenu
