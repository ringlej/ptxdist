menuconfig LTP_COMMANDS
	bool
	prompt "Commands            "
	depends on LTP
	depends on BROKEN
	help
	  Test user land commands. 

comment "LTP_COMMANDS is broken, please send a patch"
	depends on !BROKEN
