config GTK_ENGINE_EXPERIENCE
	tristate
	prompt "experience engine"
	depends on GTK_ENGINES
	help
	  A pixmap engine that is somewhat like XP, but way better...

source "rules/gtk-theme-experience.in"

