## SECTION=hosttools_noprompt
## SECTION=hosttools_platform

config HOST_IMA_EVM_UTILS
	tristate
	default y if ALLYES
	select HOST_ATTR
	select HOST_OPENSSL
	select HOST_KEYUTILS
	help
	  Integrity Measurement Architecture to know EXACTLY what has
	  been run on your machine.