menu "procps              "

config PROCPS
	bool
	prompt "Compile procps"

config PROCPS_TOP
	bool
        prompt "top"
	depends on PROCPS

config PROCPS_SLABTOP
	bool
	prompt "slabtop"
	depends on PROCPS

config PROCPS_SYSCTL
	bool
	prompt "sysctl"
	depends on PROCPS

config PROCPS_PS
	bool
	prompt "ps"
	depends on PROCPS

config PROCPS_W
	bool
	prompt "w"
	depends on PROCPS

config PROCPS_PGREP
	bool
	prompt "pgrep"
	depends on PROCPS
endmenu
