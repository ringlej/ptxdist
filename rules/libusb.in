menuconfig LIBUSB
	tristate
	prompt "libusb                        "

if LIBUSB

config LIBUSB_DOCS
	bool
	depends on LIBUSB
	prompt "Documentation"
	help
	  Activate to build the libusb-documentation

endif
