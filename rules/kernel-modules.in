## SECTION=core

menuconfig KERNEL_MODULES
	bool
    default y
	prompt "kernel-modules                "

if KERNEL_MODULES

choice
	prompt "install kernel modules"
    default KERNEL_MODULES_ALL
	help
	  FIXME

config KERNEL_MODULES_ALL
	bool
	prompt "all"
	help
	  FIXME

config KERNEL_MODULES_USER_SPEC
	bool
	prompt "selected from spec file"
	help
	  FIXME

endchoice

if KERNEL_MODULES_USER_SPEC

config KERNEL_MODULES_USER_SPEC_FILE
	string
	prompt "kernel module spec file"
    default "${PTXDIST_WORKSPACE}/kernel_modules"
    help
      FIXME

endif

endif
