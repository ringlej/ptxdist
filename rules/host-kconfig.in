config HOST_KCONFIG
        bool
        default N
        depends on BROKEN
        prompt "HOST_KCONFIG"
	help
	  FIXME: This item needs to be documented
	

comment "HOST_KCONFIG is broken, please send a patch"
        depends on !BROKEN
