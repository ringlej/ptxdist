## SECTION=scientific
config LIBMODBUS
	tristate
	prompt "libmodbus"
	help
	  libmodbus is a library that implements the Modbus/RTU protocol
	  (TCP and serial).

	  WWW: https://launchpad.net/libmodbus

