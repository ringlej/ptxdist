comment "------ module_template stuff ------"

config MODULE_TEMPLATE
	bool
	prompt "build module_template"
	default y
	select KERNEL
	help
	  Enable this entry to build the template kernel
	  driver. Note: To build the driver a kernel must
	  already have been build. That's why this target
	  depends on KERNEL. And the kernel configuration
	  must support modules at all.
