config XORG_LIB_FONTENC
	bool
	select ZLIB
	select XORG_PROTO_X
	prompt "libfontenc"
	help
	  libfontenc is a library which helps font libraries
	  portably determine and deal with different encodings
	  of fonts.
