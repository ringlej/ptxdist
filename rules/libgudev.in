## SECTION=system_libraries

config LIBGUDEV
	tristate
	select GLIB
	select UDEV
	select UDEV_LIBUDEV
	select UDEV_LIBGUDEV	if !SYSTEMD
	prompt "libgudev"
	help
	  GObject-based wrapper library for libudev
