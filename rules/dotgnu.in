# FIXME: There is no makefile for this stuff. Broken Package ?

menuconfig DOTGNU
	bool "DotGNU              "
		
config TREECC
	bool
	depends on DOTGNU && BROKEN
	prompt "treecc"

comment "treecc is broken, please send a patch"
	depends on !BROKEN


source "rules/pnet.in"
source "rules/pnetlib.in"
