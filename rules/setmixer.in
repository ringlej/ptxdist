config SETMIXER
	bool
	prompt "setmixer"
	help
	  Got bored resetting soundcard manually after every reboot?
	  Here is a small utility which can help you to avoid that.
	  The whole source is setmixer.c.

