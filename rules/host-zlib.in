config HOST_ZLIB
        bool
        default N
        depends on BROKEN
        prompt "HOST_ZLIB"
	help
	  FIXME: This item needs to be documented
	

comment "HOST_ZLIB is broken, please send a patch"
        depends on !BROKEN
