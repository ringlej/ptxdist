config QT
	bool
	depends on BROKEN
	prompt "Qt [BROKEN]"
	help
	  Qt: Cross-Platform Rich Client Development Framework

	  Qt sets the standard for high-performance,
	  cross-platform application development.  It includes
	  a C++ class library and tools for cross-platform
	  development and internationalization.

