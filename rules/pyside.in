## SECTION=qt

config PYSIDE
	tristate
	prompt "pyside-qt4"
	# not yet released for Qt 4.8
	depends on BROKEN
	select QT4
	select SHIBOKEN
	select HOST_CMAKE
	help
	  PySide provides Python bindings for the Qt cross-platform
	  application and UI framework.
