menu    "tcpdump             "

config TCPDUMP
        bool
        prompt "tcpdump"
	select LIBPCAP

config TCPDUMP_DISABLE_CRYPTO
	bool
	prompt "disable crypto support"
	depends on TCPDUMP

endmenu
