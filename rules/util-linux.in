menu "util-linux          "

config UTLNX
	bool
	prompt "Compile Util-Linux"

config UTLNX_MKSWAP
	bool
        prompt "mkswap"
	depends on UTLNX

config UTLNX_SWAPON
	bool
	prompt "swapon"
	depends on UTLNX

config UTLNX_IPCS
	bool
	prompt "ipcs"
	depends on UTLNX

config UTLNX_READPROFILE
	bool
	prompt "readprofile"
	depends on UTLNX

source "rules/clock.in"
endmenu
