# 
# /etc/inittab
#
# Generic i386 configuration
#
# 2003/08/15 Robert Schwebel <r.schwebel@pengutronix.de>
#

::sysinit:/bin/echo 
::sysinit:/etc/init.d/rcS
#::sysinit:/sbin/syslogd
#::sysinit:/sbin/syslogd host:514 (for remote logging)
#::sysinit:/sbin/sshd -f /etc/sshd_config

# Start userland network services 
::respawn:/sbin/utelnetd

# put a getty on a serial line (for a terminal)
#::respawn:/sbin/getty -L ttyS0 9600 vt100

# Start an "askfirst" shell on the console
::askfirst:-/bin/sh

# Start X11R6 TinyX server
::respawn:/usr/X11R6/bin/xinit /start_m -- /usr/X11R6/bin/Xfbdev -br -once -screen 640x240 -mouse /dev/misc/psaux

# Stuff to do before rebooting
::ctrlaltdel:/sbin/reboot
::shutdown:/bin/umount -a -r

